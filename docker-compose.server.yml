services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  server:
    image: ghcr.io/fueanta/coup-online:latest
    restart: unless-stopped
    ports:
      - "8008:8008"
    environment:
      - EXPRESS_PORT=8008
      - REDIS_CONNECTION_STRING=redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy

  watchtower:
    image: containrrr/watchtower:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME}/.docker/config.json:/config.json
    command: --interval 300 --cleanup coup-online-server-1
    environment:
      - WATCHTOWER_POLL_INTERVAL=300

volumes:
  redis-data:
